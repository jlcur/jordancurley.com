---
const { url, title, publishDate } = Astro.props;
---

<a href={url}>
  <li class="flex justify-between hover:underline rounded">
    <span class="font-semibold">{title}</span>
    <span>{publishDate}</span>
  </li>
</a>
